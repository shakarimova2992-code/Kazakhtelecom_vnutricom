/* ==========================================================================
   ЕСТЬ КОНТАКТ! — конфигурация фронтенда

   AI_ENDPOINT — адрес вашей серверной функции (см. server/cloudflare-worker.js
   и инструкцию в README.md, раздел "Подключение настоящего ИИ").

   Пока это поле пустое (""), сайт использует ТОЛЬКО шаблонный генератор
   (js/postGenerator.js) — он работает без сервера, но не переводит и не
   "сглаживает" текст по-настоящему.

   После того как вы развернёте Cloudflare Worker и получите его адрес
   (вида https://est-kontakt-ai.ваш-аккаунт.workers.dev), вставьте его сюда
   между кавычками — и панель модератора начнёт использовать настоящую
   генерацию через Claude, с автоматическим откатом на шаблон при ошибке.
   ========================================================================== */

const AI_ENDPOINT = "";

/* ==========================================================================
   GOOGLE_SHEETS_URL — адрес вашего Apps Script веб-приложения
   (см. AppsScript_DataStore_Backend.gs и инструкцию в его шапке).

   Пока это поле пустое (""), сайт хранит заявки в localStorage браузера —
   заявки видны только на том устройстве, где их подали.

   После разворачивания Apps Script вставьте сюда полученную ссылку вида
   https://script.google.com/macros/s/AKfycb.../exec — и все заявки будут
   сохраняться в Google Таблице и видны всем сотрудникам и модератору,
   независимо от устройства и браузера.
   ========================================================================== */
const GOOGLE_SHEETS_URL = "https://script.google.com/macros/s/AKfycbzwuECcuQ3Qpha5V-j-rlkKK3S1En6l5-NWbp-r8ZHBsL-S_JMLVk8qg1SwZ0zz39UX2A/exec";
